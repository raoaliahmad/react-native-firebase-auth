import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const Three = ({ navigation }) => {
	return (
		<SafeAreaView>
			<Text> Hello I'm Page Three </Text>
		</SafeAreaView>
	)
}
export default Three