import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
	Button,
} from 'react-native';

const Two = ({ navigation }) => {
	return <>
		<SafeAreaView>
			<Text>This is Page Two </Text>
		</SafeAreaView>
	</>
}
export default Two